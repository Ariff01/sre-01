pipeline {
    agent any 
    stages {
        stage('Clone the repo') {
            steps {
              echo  "Clone the repo"
                sh 'git clone https://github.com/Ariff01/wiley-sre-01.git'
            }
        }
    }
}
